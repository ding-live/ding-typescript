/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { otpCheck } from "../../funcs/otpCheck.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.CreateCheckRequest$inboundSchema.optional(),
};

export const tool$otpCheck: ToolDefinition<typeof args> = {
  name: "otp-check",
  description: `Check a code`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await otpCheck(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
